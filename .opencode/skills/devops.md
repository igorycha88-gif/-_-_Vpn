# НАВЫК: DevOps

> **РОЛЬ**: Финальная пересборка Docker, прогон ВСЕХ тестов в сборе, smoke-проверка, подведение итогов.
> **СТАТУС**: ОБЯЗАТЕЛЕН К ИСПОЛНЕНИЮ после успешного тестирования.
> **ВХОД**: Подтверждённый отчёт о тестировании от Тестировщика (багов нет, make verify пройден).
> **ВЫХОД**: Пересобранные сервисы с подтверждёнными тестами + итоговый отчёт итерации.

---

## Фундаментальное правило: Полный цикл

**DevOps этап ОБЯЗАН:**
1. Пересобрать Docker с нуля (`--no-cache`)
2. Прогнать ВСЕ тесты после пересборки
3. Проверить что все контейнеры Up
4. Проверить что API отвечает

**Если хотя бы один шаг падает — задача НЕ закрыта.**

---

## Обязательные практики (ВСЕ пункты обязательны)

### Практика 1: Анализ входных данных

DevOps **ОБЯЗАН**:

1. Получить отчёт о тестировании от Тестировщика с подтверждением отсутствия багов
2. Убедиться что Тестировщик подтвердил `make verify` пройден
3. Получить список всех изменённых файлов от Разработчика
4. Понять какие сервисы затронуты (backend, frontend, landing, nginx, sing-box)
5. Изучить `docker-compose.yml` и структуру проекта

### Практика 2: Полная пересборка сервисов

DevOps **ОБЯЗАН** пересобрать **ВСЕ** затронутые сервисы локально:

```bash
docker compose down
docker compose build --no-cache
docker compose up -d
```

Для каждого сервиса проверить:
- [ ] Контейнер запустился (статус `Up`)
- [ ] Нет ошибок в логах контейнера
- [ ] Порт(ы) пробрасываются корректно
- [ ] Healthcheck проходит (если настроен)

### Практика 3: Прогон ВСЕХ тестов после пересборки (ХАРДГЕЙТ)

DevOps **ОБЯЗАН** после успешной сборки и запуска контейнеров:

```bash
make verify
```

**Результат:**
- ✅ ВСЕ проверки пройдены → продолжить smoke-проверку
- ❌ Любая проверка падает → **ОСТАНОВКА, эскалация к пользователю**

Тесты должны пройти ВНУТРИ собранных Docker-контейнеров.

### Практика 4: Smoke-проверка (интеграционная)

DevOps **ОБЯЗАН** выполнить интеграционную проверку всех сервисов в сборе:

#### 4.1. Проверка связки сервисов

```
Nginx (:443)
  ├── / → Landing Page (статический сайт)
  ├── /api/v1/* → Go Backend API (:8080)
  └── /admin/* → React SPA (:3000)
```

Проверки:
- [ ] Landing Page доступен по корневому URL
- [ ] API Backend отвечает на `/api/v1/` эндпоинты
- [ ] Frontend SPA загружается по `/admin/`
- [ ] CORS настроен корректно между Frontend и Backend
- [ ] Статические ресурсы Frontend загружаются (JS, CSS)

#### 4.2. Проверка Docker Compose

- [ ] `docker compose up -d` — все сервисы стартуют без ошибок
- [ ] `docker compose ps` — все сервисы в статусе Up
- [ ] `docker compose logs [service]` — нет ошибок в логах
- [ ] Контейнеры могут общаться по сети
- [ ] Volumes монтируются корректно
- [ ] Переменные окружения подхватываются

#### 4.3. Проверка нового функционала

- [ ] Конкретный функционал из задачи работает в полной сборке
- [ ] API-эндпоинты отвечают корректно через Nginx
- [ ] Frontend корректно взаимодействует с Backend

#### 4.4. Проверка стабильности

- [ ] Перезапуск одного сервиса не ломает остальные
- [ ] `docker compose restart` — сервисы корректно перезапускаются
- [ ] Нет утечек памяти в логах

### Практика 5: Проверка Dockerfile

DevOps **ОБЯЗАН** для каждого затронутого Dockerfile проверить:

1. **Многоэтапная сборка** (multi-stage build) — для минимизации размера образа
2. **Минимальный базовый образ** — `alpine` или `scratch` где возможно
3. **Нет секретов в образе** — пароли, ключи, токены не попали в образ
4. **Правильный порядок слоёв** — часто меняющиеся слои внизу
5. **.dockerignore** — исключены ненужные файлы
6. **Non-root пользователь** — приложение работает не от root (где возможно)

### Практика 6: Проверка конфигурации

DevOps **ОБЯЗАН** проверить:

1. **docker-compose.yml**:
   - Версия API актуальна
   - Сервисы правильно связаны (depends_on)
   - Порты не конфликтуют
   - Volumes правильно описаны
   - Переменные окружения корректны
   - Restart policy указана
   - Resource limits указаны (где необходимо)

2. **Nginx конфигурация**:
   - Reverse proxy настроен корректно
   - SSL/TLS параметры актуальны
   - Проксирование на правильные порты
   - WebSocket поддержка (если нужна)
   - Gzip сжатие включено

3. **.env файл**:
   - Все переменные из `.env.example` определены
   - Нет реальных секретов в `.env` (только в `.env.example` с placeholder-ами)
   - `.env` добавлен в `.gitignore`

### Практика 7: Логирование и мониторинг

DevOps **ОБЯЗАН**:

1. Проверить что логи сервисов доступны:
   ```bash
   docker compose logs --tail=100 [service]
   ```
2. Убедиться что логи структурированы и читаемы
3. Проверить ротацию логов (log driver настройка)
4. Убедиться что в логах нет секретов (пароли, ключи, токены)

### Практика 8: Формирование итогового отчёта

DevOps **ОБЯЗАН** оформить итоговый отчёт итерации:

```markdown
## Итоговый отчёт итерации — DevOps

### Задача: [Название]
### ID: [идентификатор]
### Дата: [дата]

### 0. make verify: ПРОВЕРЕН — ВСЕ тесты зелёные

### 1. Сборка сервисов

| Сервис | Статус сборки | Размер образа | Статус запуска |
|---|---|---|---|
| backend | OK / FAIL | [размер] | OK / FAIL |
| frontend | OK / FAIL | [размер] | OK / FAIL |
| landing | OK / FAIL | [размер] | OK / FAIL |
| nginx | OK / FAIL | [размер] | OK / FAIL |
| singbox | OK / FAIL | [размер] | OK / FAIL |

### 2. Тесты после пересборки
- make verify: [OK / FAIL]
- Backend tests: [количество] passed, [количество] failed
- Frontend tests: [количество] passed, [количество] failed

### 3. Интеграционная проверка

- [ ] Landing Page доступен
- [ ] API Backend отвечает
- [ ] Frontend SPA загружается
- [ ] Связка Frontend ↔ Backend работает
- [ ] Nginx маршрутизация корректна

### 4. Логи сервисов

| Сервис | Ошибки | Предупреждения |
|---|---|---|
| backend | [количество] | [количество] |
| frontend | [количество] | [количество] |
| nginx | [количество] | [количество] |

### 5. Проблемы и решения

| Проблема | Решение | Статус |
|---|---|---|
| [описание] | [что сделано] | OK / FAIL |

### 6. Итоги

**Статус итерации: [УСПЕШНО / ЕСТЬ ПРОБЛЕМЫ]**

**Реализовано:**
- [краткий список что было сделано]

**Изменённые файлы:**
- [список всех изменённых/созданных файлов]

**Следующие шаги:**
- [что нужно сделать в следующей итерации]

### Чеклист:
- [ ] make verify пройден после пересборки
- [ ] Все сервисы пересобраны (--no-cache)
- [ ] Все контейнеры запущены без ошибок
- [ ] Интеграционная проверка пройдена
- [ ] Логи проверены (нет ошибок, нет утечек секретов)
- [ ] Dockerfile проверены
- [ ] Конфигурация проверена
- [ ] Итоговый отчёт оформлен
```

---

## Анти-паттерны (ЗАПРЕЩЕНО)

1. Не пересобирать сервисы после изменений
2. Не прогонять `make verify` после пересборки
3. Игнорировать ошибки в логах контейнеров
4. Не проверять интеграцию между сервисами
5. Пропускать проверку Dockerfile на безопасность
6. Коммитить секреты в docker-compose или Dockerfile
7. Не проверять связку Nginx → Backend → Frontend
8. Принимать нестабильные контейнеры (падающие при старте)
9. Не оформлять итоговый отчёт
10. Оставлять контейнеры с ошибками без внимания
11. Не проверять что .env не содержит реальных секретов
12. Закрывать задачу если `make verify` падает после пересборки

---

## Контрольный список перед завершением

- [ ] Входные данные от Тестировщика изучены (багов нет, make verify подтверждён)
- [ ] Все затронутые сервисы пересобраны (`docker compose build --no-cache`)
- [ ] Все контейнеры запущены (`docker compose up -d`)
- [ ] Статус контейнеров проверен (`docker compose ps`) — все Up
- [ ] **`make verify` пройден после пересборки**
- [ ] Логи проверены (`docker compose logs`) — нет ошибок
- [ ] Интеграционная проверка выполнена (Nginx → Backend → Frontend)
- [ ] Новый функционал работает в полной сборке
- [ ] Dockerfile проверены (безопасность, оптимизация)
- [ ] Конфигурация проверена (docker-compose, nginx, .env)
- [ ] Нет секретов в логах и образах
- [ ] Итоговый отчёт итерации оформлен
